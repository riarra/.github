name: Notify Telegram on Push (reusable)

on:
  workflow_call:
    secrets:
      NOTIFY_BOT_TOKEN:
        required: false
      NOTIFY_CHAT_ID:
        required: false

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Minimal test - just proving it runs"
